module.exports = {
  plugins: [require("daisyui")],
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      fontFamily: {
        newake: ["Newake", "sans-serif"],
      },
    },
  },
  plugins: [require("daisyui")],
};
